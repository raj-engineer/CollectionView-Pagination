# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CollectionView-Pagination' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CollectionView-Pagination
pod 'SDWebImage'

  target 'CollectionView-PaginationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CollectionView-PaginationUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
